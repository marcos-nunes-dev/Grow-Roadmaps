import styled from 'styled-components';
import { Box } from 'components/Box';

export const Wrapper = styled(Box)`
    display: flex;
    align-items: center;
    img {
        height: auto;
        width: 40px;
        margin-right: 10px;
    }
`;
