type User {
  id: ID!
  name: String
  pictureUrl: String
  email: String
  points: String
  asAdmin: Admin
}

type Query {
  me: User
  userById(userId: String!): User
  userByEmail(userEmail: String!): User
}
